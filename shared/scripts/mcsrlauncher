#!/usr/bin/env bash
set -euo pipefail

jar="$HOME/MCSR/CrossDisplayManager/MCSRlauncher/MCSRLauncher.jar"

if [ ! -f "$jar" ]; then
  echo "No MCSR Launcher jar found at: $jar" >&2
  exit 1
fi

cd "$HOME"
exec /usr/bin/java -jar "$jar"
