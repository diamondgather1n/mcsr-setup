#!/bin/sh

# Close existing Ninjabrain window if it exists
i3-msg '[class="^ninjabrainbot-Main$"] kill' >/dev/null 2>&1

# Fallback kill in case i3 did not catch it
pkill -f 'Ninjabrain-Bot-1.5.2.jar' 2>/dev/null

sleep 0.4

# Open a fresh Ninjabrain Bot
exec "$HOME/.local/bin/ninjabrain"
