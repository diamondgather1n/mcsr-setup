#!/bin/sh

if [ -n "${WAYLAND_DISPLAY:-}" ] && command -v foot >/dev/null 2>&1; then
    exec foot -e yazi "$@"
fi
if [ -n "${DISPLAY:-}" ] && command -v xterm >/dev/null 2>&1; then
    exec xterm -e yazi "$@"
fi

echo "No supported graphical terminal is available." >&2
exit 127
