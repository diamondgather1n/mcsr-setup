#!/usr/bin/env bash
set -Eeuo pipefail

if [ "$#" -ne 1 ] || [ ! -f "$1" ]; then
  echo "Choose one text file before using Edit Right." >&2
  exit 1
fi

if [ -n "${ZELLIJ_SESSION_NAME:-}" ]; then
  exec zellij action edit --direction right --cwd "$PWD" "$1"
fi

exec micro "$1"
